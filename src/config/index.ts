export const DEFAULT_PORT = 7007;

export enum StatusType {
    UP = 'up',
    WARNING = 'warning',
    CRITICAL = 'critical',
    DOWN = 'down'
}
export enum ThresholdType {
    WARNING = 5000,
    CRITICAL = 5000
}
